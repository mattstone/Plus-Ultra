<p style="color: green"><%= notice %></p>

<h1>Orders</h1>

<div class="row">
  <div class="col-1">Order No.</div>
  <div class="col-2">Amount</div>
  <div class="col-2">Status</div>
  <div class="col-2">Actions</div>
  </div>
</div>

<div id="orders">
  <% @orders.each do |order| %>
    <%= render order %>
  <% end %>
</div>

<%= link_to "New order", new_admin_order_path %>
