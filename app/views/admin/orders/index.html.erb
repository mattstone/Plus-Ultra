<p style="color: green"><%= notice %></p>

<h1>Orders</h1>

<div class="row">
  <div class="col-1 <%= admin_background_class %>">No.</div>
  <div class="col-2 <%= admin_background_class %>">Amount</div>
  <div class="col-2 <%= admin_background_class %>">Status</div>
  <div class="col-2 <%= admin_background_class %>">Actions</div>
  </div>
</div>

<div id="orders">
  <% @orders.each do |order| %>
    <%= render order %>
  <% end %>
</div>

<%#= link_to "New order", new_admin_order_path %>
